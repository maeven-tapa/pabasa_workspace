(() => {
 const data=JSON.parse(document.querySelector('#material')?.textContent||'{}'),completed=JSON.parse(document.querySelector('#completion')?.textContent||'{}'),stage=document.querySelector('#stage'),progress=document.querySelector('#progress'),fill=document.querySelector('#progressFill'),navStepper=document.querySelector('.clap-count-title'),items=[...(data.items||[])];
 if(data.randomize_order&&!completed.completed)for(let i=items.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[items[i],items[j]]=[items[j],items[i]]}
 const sound=new Audio('/static/pabasa_app/audio/clap/clap-sound-effect.mp3'),esc=x=>String(x??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])),csrf=()=>document.cookie.match(/(?:^|; )csrftoken=([^;]+)/)?.[1]||'';let index=0,phase='listen',claps=0,choice=null,answers=[],lottie=null,speaking=false,started=Date.now();
 const item=()=>items[index],word=()=>String(item()?.word||'').toUpperCase(),parts=()=>item()?.syllables||[],mascot=()=>`<img class="clap-count-mascot" src="${esc(window.CLAP_MASCOT_URL)}" alt="PABASA mascot">`,card=x=>`<article class="clap-count-card game-card-container">${x}${mascot()}</article>`;
 function steps(){const s=[['listen','👂','Listen'],['clap','👏','Clap'],['answer','🔢','Count']],p={listen:0,clap:1,answer:2}[phase]??2;return `<span class="clap-count-stepper stepper-container" aria-label="Learning steps">${s.map(([n,i,l],x)=>`<span class="clap-count-step ${x<p?'is-complete':x===p?'is-active':''}"><span class="clap-count-step-icon stepper-node-icon">${x<p?'✓':i}</span><span class="stepper-node-label">${l}</span></span>${x<2?`<span class="clap-count-step-line ${x<p?'is-complete':''}"></span>`:''}`).join('')}</span>`}
 function panel(instruction,showWord=true,wordClass=''){const characters=word().replace(/\s/g,'').length;const lengthClass=characters>14?'is-very-long':characters>9?'is-long':characters>6?'is-medium':'is-short';return `${showWord?`<div class="clap-count-word-display ${lengthClass}"><h2 class="clap-count-word ${wordClass}">${esc(word())}</h2></div>`:''}<p class="clap-count-instruction">${instruction}</p>`}
 function chunks(){return `<div class="clap-count-syllables" aria-label="Syllable chunks">${parts().map((p,i)=>`<span class="clap-count-syllable ${i<claps?'is-complete':''} ${i===claps?'is-active':''}">${esc(p)}${i<claps?'<span class="clap-count-syllable-check" aria-label="Complete">✓</span>':''}</span>`).join('')}</div>`}
 function update(){progress.textContent=`Word ${Math.min(index+1,items.length)} of ${items.length}`;fill.style.width=`${items.length?((index+1)/items.length)*100:100}%`;navStepper.classList.add('top-nav-bar');navStepper.innerHTML=steps()}
 function render(){if(!item())return finish();update();let body='';if(phase==='listen')body+=`${panel('Hear the word, then clap its spoken chunks.',true,'clap-count-word-listen-phase')}<button class="clap-count-button clap-count-listen" id="listen" type="button">🔊 Hear the word</button>`;else if(phase==='clap')body+=`${chunks()}<button class="clap-count-clap-button" id="clap" type="button" aria-label="Clap once. ${claps} claps so far"><span class="clap-count-ripple"></span><span id="clapAnimation"></span><span class="clap-count-clap-label">TAP TO CLAP</span></button><p class="clap-count-claps" id="clapCount">${claps} ${claps===1?'clap':'claps'}</p><div class="clap-count-actions"><button class="clap-count-secondary" id="clear" ${claps?'':'disabled'}>Start over</button><button class="clap-count-button" id="count" ${claps?'':'disabled'}>Count my claps →</button></div>`;else{const buttons=[1,2,3,4,5].map(n=>`<button class="clap-count-answer ${choice===n?'is-selected':''}" data-answer="${n}" aria-pressed="${choice===n}"><small>SYLLABLES</small>${n}</button>`).join('');body+=`${panel('How many syllables did you clap?')}<div class="clap-count-answer-grid" aria-label="How many syllables did you hear?">${buttons}</div><div class="clap-count-actions"><button class="clap-count-secondary" id="again">Clap again</button><button class="clap-count-button" id="check" ${choice===null?'disabled':''}>Check my answer ✓</button></div>`}stage.innerHTML=card(`<div class="clap-count-content">${body}</div>`);if(phase==='clap')setup();document.querySelector('#listen')?.addEventListener('click',speak);document.querySelector('#clap')?.addEventListener('click',clap);document.querySelector('#clear')?.addEventListener('click',()=>{claps=0;render()});document.querySelector('#count')?.addEventListener('click',()=>{phase='answer';render()});document.querySelector('#again')?.addEventListener('click',()=>{phase='clap';choice=null;render()});document.querySelectorAll('[data-answer]').forEach(b=>b.addEventListener('click',()=>{choice=+b.dataset.answer;render()}));document.querySelector('#check')?.addEventListener('click',check)}
 function speak(){if(speaking)return;if(!('speechSynthesis'in window)){phase='clap';return render()}speaking=true;const b=document.querySelector('#listen');b.disabled=true;b.classList.add('is-playing');b.textContent='🔊 Listening…';const w=document.querySelector('.clap-count-word');w?.classList.add('is-listening');speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(item().word);u.lang=data.language==='Filipino'?'fil-PH':'en-US';const done=()=>{speaking=false;if(phase==='listen'){phase='clap';render()}};u.onend=done;u.onerror=done;speechSynthesis.speak(u)}
 function setup(){const c=document.querySelector('#clapAnimation');if(!c||!window.lottie)return;lottie?.destroy();lottie=window.lottie.loadAnimation({container:c,renderer:'svg',loop:false,autoplay:false,path:window.CLAP_ANIMATION_URL})}
 function clap(){if(claps>=10)return;claps++;document.querySelector('#clapCount').textContent=`${claps} ${claps===1?'clap':'claps'}`;const b=document.querySelector('#clap'),c=stage.querySelector('.clap-count-card');b.classList.remove('is-clapping');void b.offsetWidth;b.classList.add('is-clapping');c.classList.add('is-clapping');setTimeout(()=>{b.classList.remove('is-clapping');c.classList.remove('is-clapping')},430);sound.currentTime=0;sound.play().catch(()=>{});lottie?.stop();lottie?.goToAndPlay(0,true);document.querySelector('#clear').disabled=false;document.querySelector('#count').disabled=false;stage.querySelector('.clap-count-syllables').outerHTML=chunks()}
 function check(){if(choice===null)return;answers.push({word_id:item().id,answer:choice,claps});if(choice===+item().syllable_count)return success();retry()}
 function retry(){update();stage.innerHTML=card(`<div class="clap-count-content"><section class="clap-count-success"><h2>Let’s clap again</h2><p><strong>${esc(word())}</strong></p>${chunks()}<button class="clap-count-button" id="retry">👏 Try clapping again</button></section></div>`);document.querySelector('#retry').onclick=()=>{claps=0;choice=null;phase='clap';render()}}
 function success(){update();stage.innerHTML=card(`<div class="clap-count-content"><section class="clap-count-success"><div class="clap-count-success-banner">✨ GREAT JOB! ✨</div><h2 class="clap-count-success-title">Amazing clapping!</h2><p class="clap-count-success-word">${esc(word())}</p><div class="clap-count-result-syllables" aria-label="Syllable breakdown">${parts().map(part=>`<span class="clap-count-result-chip">${esc(part)}</span>`).join('')}</div><p class="clap-count-syllable-total">👏 ${item().syllable_count} ${+item().syllable_count===1?'syllable':'syllables'} 👏</p><p class="clap-count-mascot-cheer">🐧 You crushed it!</p><button class="clap-count-button clap-count-success-next" id="next">${index===items.length-1?'Finish':'Next word →'}</button></section></div>`);document.querySelector('#next').onclick=()=>{index++;phase='listen';claps=0;choice=null;render()}}
 async function finish(){progress.textContent='Complete ✓';fill.style.width='100%';stage.innerHTML=card(`<div class="clap-count-content"><section class="clap-count-success"><div class="clap-count-phase">Saving result…</div><h2>Great job! 🎉</h2><p>You completed Clap &amp; Count Syllables.</p></section></div>`);try{const r=await fetch('/record-assessment-completion/',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRFToken':csrf()},body:JSON.stringify({material_id:data.id,activity_type:'assessment',assessment_type:'word',items_completed:items.length,scores:{answers,duration_seconds:Math.round((Date.now()-started)/1000)}})}),result=await r.json();if(!r.ok||!result.success)throw Error(result.error||'Unable to save result.');stage.innerHTML=card(`<div class="clap-count-content"><section class="clap-count-success"><div class="clap-count-phase">Activity complete</div><h2>Great job! 🎉</h2><p>${+result.correct_items||0} of ${+result.items_completed||0} correct · ${+result.accuracy||0}%</p><a class="clap-count-button" href="/dashboard/assessment/">Back to Assessments →</a></section></div>`)}catch(e){stage.innerHTML=card(`<div class="clap-count-content"><section class="clap-count-success"><h2>Please try again</h2><p>${esc(e.message)}</p><button class="clap-count-button" id="save">Save again</button></section></div>`);document.querySelector('#save').onclick=finish}}
 if(completed.completed){progress.textContent='Completed ✓';fill.style.width='100%';stage.innerHTML=card(`<div class="clap-count-content"><section class="clap-count-success"><div class="clap-count-phase">Completed ✓</div><h2>Activity finished!</h2><p>${+completed.correct_items||0} of ${+completed.total_items||0} correct · ${+completed.accuracy||0}%</p><a class="clap-count-button" href="/dashboard/assessment/">Back to Assessments →</a></section></div>`)}else if(items.length)render();
})();
