(()=>{'use strict';const n=document.getElementById('prescribed-activity-data'),app=document.getElementById('app');if(!n||!app)return;const d=JSON.parse(n.textContent||'{}'),csrf=()=>((document.cookie.match(/(?:^|; )csrftoken=([^;]+)/)||[])[1]||''),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let s={...(d.progress?.state||{})},busy=false,audioUrl=null,audio=null;function hydrate(){s.current_item=Number(s.current_item||0);s.selected_words||=[];s.phase||='spotting'}async function post(url,body){const r=await fetch(url,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRFToken':csrf()},body:JSON.stringify(body)}),x=await r.json();if(!r.ok||!x.success)throw Error(x.error||'Could not save your progress.');if(x.progress?.state)s={...x.progress.state};hydrate();return x}function steps(){const current=s.phase==='complete'?d.words.length:s.current_item;return `<div class="progress">${d.words.map((_,i)=>`<span class="step ${i<current?'done':''} ${i===current&&s.phase!=='complete'?'active':''}">${i+1}</span>`).join('')}</div>`}function render(message='',kind=''){hydrate();if(s.phase==='complete'||s.current_item>=d.words.length){app.innerHTML=`<div class="complete">🎉 Great job! You spotted all the words.</div>${steps()}`;post(d.completion_url,{}).catch(()=>{});return}const selected=new Set(s.selected_words);app.innerHTML=`<div class="eyebrow">SESSION 13 · LESSON 29 · ACTIVITY 2</div><h1 class="title">Spot the Word</h1><p class="instruction">Listen to the word, then encircle it.</p><div class="grid">${d.words.map(w=>`<button class="word ${selected.has(w)?'correct':s.wrong_word===w?'wrong':''}" data-word="${esc(w)}" ${selected.has(w)||busy?'disabled':''}>${esc(w)}</button>`).join('')}</div><p class="status ${kind}">${esc(message||'Listen carefully, then choose the word you heard.')}</p><button class="button" id="listen" ${busy?'disabled':''}>🔊 Listen</button>${steps()}`;app.querySelectorAll('[data-word]').forEach(b=>b.onclick=()=>choose(b.dataset.word));document.getElementById('listen').onclick=()=>play(s.target_word).catch(e=>render(e.message,'bad'))}async function play(text){if(busy||!text)return;busy=true;try{const r=await fetch(d.read_aloud_url,{method:'POST',credentials:'same-origin',headers:{'X-CSRFToken':csrf(),'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({target_text:text,language:'English',lesson_tts_key:'lesson-29-gawain-2'})}),x=await r.json();if(!r.ok||!x.success||!x.audio_content)throw Error(x.error||'Could not play audio.');const bytes=Uint8Array.from(atob(x.audio_content),c=>c.charCodeAt(0));audioUrl=URL.createObjectURL(new Blob([bytes],{type:x.mime_type||'audio/mpeg'}));audio=new Audio(audioUrl);await new Promise((ok,bad)=>{audio.onended=ok;audio.onerror=()=>bad(Error('Audio playback failed.'));audio.play().catch(bad)})}finally{busy=false;if(audioUrl){URL.revokeObjectURL(audioUrl);audioUrl=null}audio=null}}async function begin(){await post(d.progress_url,{action:'begin',item_index:s.current_item});render()}async function choose(word){if(busy)return;busy=true;let message='',kind='',nextTarget='';try{const result=await post(d.progress_url,{action:'choose',item_index:s.current_item,word});message=result.accepted?'Correct! Listen for the next word.':'Not that word. Try again.';kind=result.accepted?'good':'bad';nextTarget=result.accepted&&s.phase!=='complete'?s.target_word:''}catch(e){message=e.message;kind='bad'}finally{busy=false}render(message,kind);if(nextTarget)setTimeout(()=>play(nextTarget).catch(e=>render(e.message,'bad')),350)}async function reset(e){e.preventDefault();if(busy)return;busy=true;try{await post(d.progress_url,{reset:true});location.assign(document.getElementById('lesson29a2-back').href)}catch(x){busy=false;alert(x.message)}}document.getElementById('lesson29a2-back').onclick=reset;document.getElementById('lesson29a2-later').onclick=reset;document.getElementById('lesson29a2-go').onclick=async()=>{document.getElementById('lesson29a2-start').hidden=true;document.getElementById('lesson29a2-stage').classList.remove('waiting');try{await play('Spot the Word. Listen to the word, then encircle it.');await begin();await play(s.target_word)}catch(e){render(e.message,'bad')}};window.addEventListener('pagehide',()=>audio?.pause());hydrate();render()})();
