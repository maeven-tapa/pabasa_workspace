(() => {
  const node=document.querySelector('#prescribed-activity-data'),app=document.querySelector('#app');if(!node||!app)return;
  const data=JSON.parse(node.textContent||'{}'),csrf=()=>((document.cookie.match(/(?:^|; )csrftoken=([^;]+)/)||[])[1]||''),esc=value=>String(value??'').replace(/[&<>]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[char]));
  let state={...(data.progress?.state||{})},busy=false,stream,flash='';const hydrate=()=>{state.current_item=Number(state.current_item||0);state.phase||='reading';};const steps=()=>`<div class="progress">${data.items.map((_,index)=>`<span class="step ${index<state.current_item?'done':''} ${index===state.current_item?'active':''}">${index+1}</span>`).join('')}</div>`;
  async function save(body){const response=await fetch(data.progress_url,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRFToken':csrf()},body:JSON.stringify(body)}),result=await response.json();if(!response.ok||!result.success)throw Error(result.error||'Could not save progress.');state={...result.progress.state};hydrate();return result;}
  async function speak(text){const response=await fetch(data.read_aloud_url,{method:'POST',credentials:'same-origin',headers:{'X-CSRFToken':csrf(),'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({target_text:text,language:'English',lesson_tts_key:'lesson-31-gawain-4'})}),result=await response.json();if(!response.ok||!result.audio_content)throw Error(result.error||'Read aloud is unavailable.');const source=URL.createObjectURL(new Blob([Uint8Array.from(atob(result.audio_content),char=>char.charCodeAt(0))],{type:'audio/mpeg'})),audio=new Audio(source);await new Promise((resolve,reject)=>{audio.onended=resolve;audio.onerror=reject;audio.play().catch(reject)});URL.revokeObjectURL(source);}
  function render(message='',kind=''){
    hydrate();
    if(state.phase==='complete'){app.innerHTML='<div class="complete">🎉 Great job! You named every picture.</div>'+steps();fetch(data.completion_url,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRFToken':csrf()},body:'{}'});return;}
    const item=data.items[state.current_item], reading=state.phase==='reading';
    const header='<div class="eyebrow">SESSION 15 · LESSON 31 · ACTIVITY 4</div><h1 class="title">Say and Circle!</h1><p class="instruction">'+(reading?'Say the name of the picture.':'Box the word that names the picture.')+'</p><div class="picture"><img src="'+esc(item.image_url)+'" alt="'+esc(item.alt_text)+'"></div>';
    let body;
    if(reading){const listen=Number(state.attempts||0)>=3?'<button class="button secondary" id="listen">🔊 Listen</button>':'';body='<p class="status '+kind+'">'+esc(message||'Say the picture word aloud.')+'</p><div class="actions"><button class="button" id="read">🎙️ Say the picture word</button>'+listen+'</div>';}
    else {const choices=item.choices.map(word=>'<button class="choice '+(flash===word?'good':'')+'" data-word="'+esc(word)+'">'+esc(word)+'</button>').join('');body='<div class="choices">'+choices+'</div><p class="status '+kind+'">'+esc(message||'Choose the correct word.')+'</p>';}
    app.innerHTML=header+body+steps();
    app.querySelector('#read')?.addEventListener('click',()=>record(item.word));app.querySelector('#listen')?.addEventListener('click',()=>speak(item.word).catch(error=>render(error.message,'bad')));app.querySelectorAll('[data-word]').forEach(button=>button.onclick=()=>choose(button.dataset.word));
  }
  async function record(target){if(busy||!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder)return render('Microphone recording is not available.','bad');busy=true;try{stream=await navigator.mediaDevices.getUserMedia({audio:true});const recorder=new MediaRecorder(stream),chunks=[];recorder.ondataavailable=event=>event.data.size&&chunks.push(event.data);const blob=await new Promise(resolve=>{recorder.onstop=()=>resolve(new Blob(chunks));recorder.start();setTimeout(()=>recorder.stop(),3000)});stream.getTracks().forEach(track=>track.stop());const form=new FormData();form.append('audio',blob,'lesson31-activity4.webm');form.append('target_text',target);form.append('language','English');const response=await fetch(data.transcribe_url,{method:'POST',headers:{'X-CSRFToken':csrf()},body:form}),transcript=await response.json();if(!response.ok||!transcript.success)throw Error(transcript.error||'Speech recognition failed.');const result=await save({action:'read_word',heard:transcript.raw_transcript||transcript.transcript});render(result.accepted?'Correct! Now box the matching word.':'Please try again.',result.accepted?'good':'bad');}catch(error){render(error.message||'Please try again.','bad')}finally{busy=false;}}
  async function choose(word){if(busy)return;busy=true;try{const result=await save({action:'choose',choice:word});if(result.accepted){flash=word;render('Correct!', 'good');setTimeout(()=>{flash='';render();},650);}else render('That is not the picture word. Read it again first.','bad');}catch(error){render(error.message,'bad')}finally{busy=false;}}
  async function leave(event){event.preventDefault();try{await save({reset:true});location.assign(document.querySelector('#lesson31a4-back').href)}catch(error){alert(error.message);}}document.querySelector('#lesson31a4-back').onclick=leave;document.querySelector('#lesson31a4-later').onclick=leave;document.querySelector('#lesson31a4-go').onclick=()=>{document.querySelector('#lesson31a4-start').hidden=true;document.querySelector('#lesson31a4-stage').classList.remove('waiting');render()};hydrate();render();
})();
