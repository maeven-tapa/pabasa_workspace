(() => {
  'use strict';
  const node = document.getElementById('prescribed-activity-data');
  const app = document.getElementById('app');
  if (!node || !app) return;
  const data = JSON.parse(node.textContent || '{}');
  const csrf = () => (document.cookie.match(/(?:^|; )csrftoken=([^;]+)/) || [])[1] || '';
  const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  const normalize = value => String(value || '').toLowerCase().replace(/[^a-z]/g, '');
  let state = {...(data.progress?.state || {})};
  let busy = false, stream = null, audioUrl = null, audio = null;
  function hydrate() { state.current_item = Number(state.current_item || 0); state.completed_items = Number(state.completed_items || 0); state.phase ||= 'reading'; }
  async function post(url, body) { const r = await fetch(url,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRFToken':csrf()},body:JSON.stringify(body)}), x = await r.json(); if (!r.ok || !x.success) throw new Error(x.error || 'Could not save your progress.'); if (x.progress?.state) state = {...x.progress.state}; hydrate(); return x; }
  function progress() { const current = state.phase === 'complete' ? data.items.length : state.current_item; return `<div class="progress" aria-label="Activity progress">${data.items.map((_,i)=>`<span class="step ${i < current ? 'done':''} ${i === current && state.phase !== 'complete' ? 'active':''}">${i+1}</span>`).join('')}</div>`; }
  function render(message='', kind='') {
    hydrate();
    if (state.phase === 'complete' || state.current_item >= data.items.length) { app.innerHTML = `<div class="complete">🎉 Great job! You completed Missing Letter.</div>${progress()}`; post(data.completion_url,{}).catch(()=>{}); return; }
    const item = data.items[state.current_item], letterPhase = state.phase === 'letter';
    app.innerHTML = `<div class="eyebrow">SESSION 12 · LESSON 28 · ACTIVITY 1</div><h1 class="title">Missing Letter</h1><p class="instruction">Read the word, then write the missing letter.</p><div class="content"><div class="item"><img class="picture" src="${esc(item.image_url)}" alt="Picture clue for ${esc(item.word)}"><div><div class="label">${letterPhase ? 'Write the missing letter' : 'Read the word aloud first'}</div><div class="stem">${letterPhase ? `<input class="letter" id="letter" maxlength="1" aria-label="Missing first letter">${esc(item.stem.slice(2))}` : esc(item.stem)}</div><p class="status ${kind}" id="status">${esc(message || (letterPhase ? 'Correct! Type the missing letter.' : 'Say the word shown in the picture.'))}</p><div class="actions">${letterPhase ? '<button class="button" id="check" type="button">Check letter</button>' : '<button class="button" id="read" type="button">🎙️ Read the word</button><button class="button secondary" id="listen" type="button">🔊 Listen</button>'}</div></div></div></div>${progress()}`;
    document.getElementById('read')?.addEventListener('click', () => record(item));
    document.getElementById('listen')?.addEventListener('click', () => playAudio(item.word).catch(error => render(error.message,'bad')));
    document.getElementById('check')?.addEventListener('click', () => submitLetter(item));
    document.getElementById('letter')?.addEventListener('keydown', event => { if (event.key === 'Enter') submitLetter(item); });
    document.getElementById('letter')?.focus();
  }
  async function playAudio(text) { if (busy || !text) return; busy=true; try { const r=await fetch(data.read_aloud_url,{method:'POST',credentials:'same-origin',headers:{'X-CSRFToken':csrf(),'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({target_text:text,language:'English'})}),x=await r.json(); if(!r.ok||!x.success||!x.audio_content) throw new Error(x.error||'Could not play audio. Try again.'); const bytes=Uint8Array.from(atob(x.audio_content),c=>c.charCodeAt(0)); if(audioUrl)URL.revokeObjectURL(audioUrl); audioUrl=URL.createObjectURL(new Blob([bytes],{type:x.mime_type||'audio/mpeg'})); audio=new Audio(audioUrl); await new Promise((resolve,reject)=>{audio.onended=resolve;audio.onerror=()=>reject(new Error('Audio playback failed. Try again.'));audio.play().catch(reject);}); } finally { busy=false; if(audioUrl){URL.revokeObjectURL(audioUrl);audioUrl=null;} audio=null; } }
  async function record(item) { if(busy)return; if(!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder){render('Microphone recording is not available in this browser.','bad');return;} busy=true; const status=document.getElementById('status'); if(status)status.textContent='Listening…'; try { stream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true}}); const recorder=new MediaRecorder(stream),chunks=[]; recorder.ondataavailable=e=>e.data.size&&chunks.push(e.data); const stopped=new Promise((resolve,reject)=>{recorder.onerror=()=>reject(new Error('Could not record your voice.'));recorder.onstop=()=>resolve(new Blob(chunks,{type:recorder.mimeType||'audio/webm'}));recorder.start();setTimeout(()=>recorder.state==='recording'&&recorder.stop(),3000);}); const blob=await stopped; stopStream(); const form=new FormData();form.append('audio',blob,'lesson28-activity1.webm');form.append('target_text',item.word);form.append('language','English');form.append('mode','reading');const response=await fetch(data.transcribe_url,{method:'POST',credentials:'same-origin',headers:{'X-CSRFToken':csrf()},body:form}),result=await response.json();if(!response.ok||!result.success)throw new Error(result.error||'Speech recognition failed. Try again.');const transcript=String(result.raw_transcript||result.transcript||'');const saved=await post(data.progress_url,{action:'oral_read',item_index:state.current_item,success:normalize(transcript).includes(normalize(item.word))});render(saved.progress?.state?.phase==='letter'?'Correct! Now write the missing letter.':`I heard “${transcript}”. Try again.`,saved.progress?.state?.phase==='letter'?'good':'bad'); } catch(error){stopStream();render(error.message||'I could not hear you. Try again.','bad');} finally{busy=false;} }
  async function submitLetter(item) { if(busy)return; const input=document.getElementById('letter'), letter=normalize(input?.value); if(letter.length!==1){render('Type one missing letter, then check your answer.','bad');return;} busy=true;try{const result=await post(data.progress_url,{action:'letter_answer',letter});render(result.accepted?'Correct!':'That letter is incorrect. Try again.',result.accepted?'good':'bad');}catch(error){render(error.message,'bad');}finally{busy=false;} }
  function stopStream(){stream?.getTracks().forEach(track=>track.stop());stream=null;}
  async function reset(event){event.preventDefault();if(busy)return;busy=true;try{await post(data.progress_url,{reset:true});window.location.assign(document.getElementById('lesson28a1-back').href);}catch(error){busy=false;window.alert(error.message||'Could not reset the activity.');}}
  document.getElementById('lesson28a1-back').addEventListener('click',reset);document.getElementById('lesson28a1-later').addEventListener('click',reset);document.getElementById('lesson28a1-go').addEventListener('click',async()=>{document.getElementById('lesson28a1-start').hidden=true;document.getElementById('lesson28a1-stage').classList.remove('waiting');try{await playAudio('Missing Letter. Read the word, then write the missing letter.');}catch(error){render(error.message,'bad');}});window.addEventListener('pagehide',()=>{stopStream();audio?.pause();});hydrate();render();
})();
